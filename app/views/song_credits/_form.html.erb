<%= form_for([@record, @song, @song_credit]) do |form| %>
  <% if @song_credit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song_credit.errors.count, "error") %> prohibited this credit from being saved:</h2>

      <ul>
      <% @song_credit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>Player: <%= form.collection_select :player_id, Player.where(user_id: current_user.id).order(:name), :id, :name, {:include_blank => 'Existing authors'} %></p>
  <p>Description: <%= form.text_field :description %></p>

  <%= form.submit %>
<% end %>